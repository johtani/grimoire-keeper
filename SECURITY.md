# セキュリティポリシー

## サポート範囲

Grimoire Keeper は、個人または少人数の信頼された利用者が管理された環境で使うことを想定しています。インターネットへ直接公開する構成、不特定多数が利用する構成、相互に信頼しない利用者を収容するマルチテナント構成は、現在のサポート範囲外です。

セキュリティ修正は原則として最新の `main` ブランチを対象とします。リリース版を運用する場合は、最新の修正を取り込めるようにしてください。

## 現在の脅威モデル

このアプリケーションは、登録された URL、取得したページ本文、要約、キーワード、メモを保存して検索できるようにします。次の点を前提に運用してください。

- API には利用者認証・認可がありません。API に到達できる利用者は、URL の登録、保存済みデータの閲覧・変更、再処理などの操作を実行できます。
- API と Web の CORS 設定は任意のオリジンを許可します。CORS はアクセス制御の代わりにはなりません。
- 本番用 Compose の Weaviate は匿名アクセスが有効です。ホストへ公開される HTTP ポート `8089` と gRPC ポート `50051` に到達できる利用者は、Weaviate を直接操作できる可能性があります。
- 本番用 Compose の Web、API、Weaviate のポート指定には待ち受けアドレスがなく、既定ではホストの全インターフェースに公開されます。そのままインターネットへ接続しないでください。
- ページ内容は外部の Jina AI Reader、LLM プロバイダー、埋め込みプロバイダーへ送信されます。各サービスのデータ処理・保持方針も確認してください。
- 取得したページは信頼できない入力です。ページ本文、タイトル、要約、キーワード、メモ、外部サービスの応答を、命令や安全な HTML として信用しないでください。

## 安全なデプロイ

安全な既定運用は、サービスをループバックインターフェース、VPN、またはアクセス制御された信頼済みネットワークのみに公開することです。

- ファイアウォールまたは Compose のポートバインドで、Web と API への接続元を信頼済みの端末に限定してください。
- Weaviate の `8089` と `50051` はホストへ公開せず、Compose の内部ネットワークからだけ接続させることを推奨します。公開が必要な場合は匿名アクセスを無効化し、認証とネットワーク制限を設定してください。
- 信頼済みネットワークの外から Web または API を利用する場合は、TLS、強固な利用者認証、アクセス制御、レート制限を備えたリバースプロキシやアクセスゲートウェイを前段に配置してください。
- API の `8000` を認証なしで直接公開しないでください。Web の認証だけでは、API や Weaviate の直接アクセスを防げません。
- ホスト OS、コンテナイメージ、依存関係を更新し、データディレクトリと設定ファイルはサービス用アカウントだけが読み書きできる権限にしてください。

## URL 取得と外部通信

API は URL 入力を `http` または `https` に制限していますが、現在はホスト名や解決後の IP アドレスに対する許可・拒否規則を実装していません。localhost、プライベートアドレス、リンクローカルアドレス、クラウドのメタデータサービス、リダイレクト後の宛先をアプリケーション側では検証しません。

ページ取得そのものは Jina AI Reader に委譲されますが、入力 URL と取得結果は外部サービスへ送信され、プロバイダーから到達できる宛先が処理対象になり得ます。また、URL に埋め込まれた認証情報や秘密のクエリ文字列が保存・送信・記録される可能性があります。

- URL 登録 API は信頼された利用者だけが利用できるようにしてください。
- 秘密情報、署名付き URL、セッショントークン、個人用イントラネットの URL を登録しないでください。
- 必要に応じて、組織の送信プロキシ、DNS、ファイアウォール、および利用する外部プロバイダー側で宛先を制限してください。
- URL 制限をセキュリティ境界として必要とする環境では、プライベートアドレスとリダイレクト先の検証を実装するまで利用しないでください。

## 保存データとログ

次のデータは機密情報として扱ってください。

- SQLite: URL、タイトル、要約、キーワード、メモ、処理状態、エラーや処理履歴
- JSON ストレージ: Jina AI Reader から取得したページ本文とメタデータ
- Weaviate: ページのチャンク、要約、メモ、メタデータ、埋め込みベクトル
- アプリケーションログとテレメトリー: URL、識別子、エラー、処理情報など

外部サービスのレスポンス本文はログへ出力しない方針ですが、URL や例外情報だけでも機密情報を含むことがあります。ログとテレメトリーの送信先を制限し、必要最小限の保持期間を設定してください。取得したデータを表示・エクスポートする機能では、信頼できない文字列としてエスケープまたは無害化してください。

データディレクトリ、ログ、テレメトリー、バックアップへのアクセスを最小権限に制限し、不要になったデータは各保存先から削除してください。SQLite または JSON だけを削除しても、Weaviate、ログ、バックアップ、外部プロバイダーに複製が残る場合があります。

## シークレット管理

- API キーやトークンは Bitwarden Secrets Manager で管理し、ソースコード、`.env`、ログ、Issue、バックアップへ含めないでください。
- `BWS_ACCESS_TOKEN` はリポジトリ外の `~/.config/bws.env` に保存し、`chmod 600` などで所有者だけが読み書きできるようにしてください。
- `.env.example` と `.env` には非秘密の設定値だけを記載してください。`.gitignore` は誤操作を完全には防げないため、コミット前にも差分を確認してください。
- サービスごとに必要最小限の権限を持つキーを使い、漏えいが疑われる場合は直ちに失効・再発行してください。

## バックアップ

災害復旧には、少なくとも SQLite、JSON ストレージ、Weaviate の永続データ、および復元に必要な非秘密設定を整合性の取れた時点でバックアップしてください。デプロイスクリプトがスキーマ変更前に作成する SQLite バックアップは、JSON や Weaviate を含まないため、完全な災害復旧バックアップではありません。

- バックアップは暗号化し、本番ホストとは別のアクセス制御された場所へ保存してください。
- バックアップ用アカウントと暗号鍵を最小権限で管理し、シークレットをバックアップへ混在させないでください。
- 保持期間と安全な削除手順を定め、不要な世代を残さないでください。
- 定期的に復元をテストし、SQLite、JSON、Weaviate 間のデータが一貫していることを確認してください。
- 旧コードへロールバックする場合は、互換性のある時点のデータ一式も復元してください。

## インシデント対応

侵害やシークレット漏えいが疑われる場合は、次の順序を目安に対応してください。

1. Web、API、Weaviate を信頼されていないネットワークから隔離し、必要ならサービスを停止する。
2. 影響を受けた BWS、Jina、LLM、OpenAI、Slack などのトークンと API キーを失効・再発行する。
3. ログ、テレメトリー、データ、バックアップを保全し、侵入経路、実行された操作、外部送信、影響範囲を調査する。
4. 修正版へ更新し、安全なバックアップから復元したうえで、ネットワーク制限と認証を確認してから再開する。
5. 外部サービスや第三者のデータが影響を受けた場合は、それぞれの契約・法令・通知手順に従う。

## 脆弱性の報告

脆弱性を発見した場合は、GitHub リポジトリの **Security** タブにある **Report a vulnerability** から非公開で報告してください。この機能が利用できない場合は、詳細を公開せず、最小限の内容でリポジトリ管理者へ非公開の連絡方法を問い合わせてください。

報告には、影響を受けるバージョンまたはコミット、再現手順、想定される影響、確認済みの回避策を含めてください。API キー、個人情報、取得したページ本文、実運用環境への攻撃結果は添付しないでください。修正と公開方針が合意されるまで、公開 Issue、Discussion、Pull Request で脆弱性の詳細を共有しないでください。
